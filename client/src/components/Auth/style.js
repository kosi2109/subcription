import {makeStyles} from "@material-ui/core"

export const useStyle = makeStyles({
    container:{
        marginTop: "1rem"
    },
    paper:{
        padding: "1rem"
    },
    typography:{
        marginBottom: "1em"
    },
    margin:{
        marginBottom: "1em"
    }
})